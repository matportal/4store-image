# matportal-4store-image

Container image build for 4store, published to GHCR.

## Image

- `ghcr.io/matportal/4store:<tag>`

## Notes

- Built natively on GitHub Actions for both `linux/amd64` and `linux/arm64`.
- No QEMU emulation is used.
